# Phase 1 — environment, Spider download, and EDA
# (training / serving deps land in later phases)

datasets>=2.19.0
huggingface_hub>=0.23.0
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=15.0.0
tqdm>=4.66.0
rich>=13.7.0
ipython>=8.25.0
streamlit>=1.35.0

# Phase 3 — tokenizer + PyTorch dataset (Unsloth/CUDA image later for training)
transformers>=4.43.0
sentencepiece>=0.2.0
protobuf>=4.25.0
torch>=2.2.0

